/**
 * Scrollable list of moves in standard checkers notation.
 * Task 2.4 implements the full component.
 */

export default function MoveHistory() {
  return <div data-testid="move-history">MoveHistory placeholder</div>;
}
