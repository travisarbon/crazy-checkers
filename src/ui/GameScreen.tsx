/**
 * Game layout: board + sidebar (captured pieces, turn indicator, controls).
 * Task 2.4 implements the full responsive layout.
 */

export default function GameScreen() {
  return <div data-testid="game-screen">GameScreen placeholder</div>;
}
