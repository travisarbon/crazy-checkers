/**
 * Board rendering — SVG-based 8x8 board with piece placement,
 * square highlighting, and move animation.
 * Task 2.1 implements the full component.
 */

export default function Board() {
  return <div data-testid="board">Board placeholder</div>;
}
